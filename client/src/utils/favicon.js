// src/utils/favicon.js
// loads favicon in development based on /public/favicon.ico but the root /flavicon.ico in production

// const faviconPath = import.meta.env.VITE_FAVICON_PATH || '/favicon.ico';

// const linkElement = document.querySelector('link[rel="icon"]') || document.createElement('link');
// linkElement.rel = 'icon';
// linkElement.href = faviconPath;

// document.head.appendChild(linkElement);
